{
  "name": "aaron-os",
  "version": "1.0.0",
  "description": "AaronOS – Unified dashboard featuring Lucy and eBook Machine",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "npx http-server . -p 3000",
    "build": "echo 'Static site - no build needed'",
    "start": "npx http-server . -p ${PORT:-8080}"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {}
}
